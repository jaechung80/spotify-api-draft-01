import React, { useState } from 'react';

function SearchForm() {
  return (
    <div>
      <form>
        <button type='submit'>Search</button>
      </form>
    </div>
  );
}

export default SearchForm;
